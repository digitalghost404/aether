# Govee API key — copy this to .env and fill in your value
# Get API key from Govee Home app → Account → Apply for API Key
# Email/password not required — API key only mode works for Aether
GOVEE_API_KEY=your-api-key-here
